# Tuvisha B — Portfolio

This is a modern, responsive React portfolio built with Vite, Tailwind CSS, Framer Motion and React Icons.

Setup

1. Install dependencies

```bash
npm install
```

2. Run dev server

```bash
npm run dev
```

Notes

- Theme is stored in localStorage.
- Tailwind dark mode uses the `class` strategy.
- Replace `resume.pdf` in the project root with your actual resume.
